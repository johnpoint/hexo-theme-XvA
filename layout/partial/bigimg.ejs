<script>
    let pagestatus = 0;
    $('.navbar-default').addClass("nav-bg");
    if (window.location.href.indexOf("page") != -1) {
        $('#bigimg').hide();
        $('.navbar-default').removeClass("nav-bg");
        pagestatus = 1;
    }
    $(window).scroll(function(event) {
        h = $(window).scrollTop()
        if (pagestatus == 0) {
            if (h >= document.body.clientHeight - 30) {
                $('.navbar-default').removeClass("nav-bg");
            }
            if (h <= document.body.clientHeight - 30) {
                $('.navbar-default').addClass("nav-bg");
            }
        }
    });

    function goindex() {
        window.scrollTo({
            top: document.body.clientHeight - 10,
            behavior: "smooth"
        })
    }
</script>
<link href="https://fonts.googleapis.com/css2?family=Titillium+Web:wght@700&display=swap" rel="stylesheet">
<div id="bg-shaw">
    <i onclick="goindex()" class="iconfont icon-down"></i>
</div>
<h1 id="bg-title">
    <%= config.title -%>
</h1>
<div id="bg-text">
    <% if(theme.bigimg.RSS!=''){ %>
        <a href="<%= theme.bigimg.RSS -%>" class="iconfont icon-rss"></a>
        <% } %>
            <% if(theme.bigimg.Github!=''){ %>
                <a href="<%= theme.bigimg.Github -%>" class="iconfont icon-git"></a>
                <% } %>
                    <% if(theme.bigimg.Telegram!=''){ %>
                        <a href="<%= theme.bigimg.Telegram -%>" class="iconfont icon-telegram"></a>
                        <% } %>
                            <% if(theme.bigimg.Mail!=''){ %>
                                <a href="mailto:<%= theme.bigimg.Mail -%>" class="iconfont icon-mail"></a>
                                <% } %>
                                    <% if(theme.bigimg.wikipedia!=''){ %>
                                        <a href="<%= theme.bigimg.wikipedia -%>" class="iconfont icon-wikipedia"></a>
                                        <% } %>
</div>
<style>
    #bg-shaw {
        height: 100%;
        width: 100%;
        background: rgba(0, 0, 0, 0.2);
    }
    
    div#bg-text {
        text-align: center;
    }
    
    i.iconfont.icon-down {
        font-size: 2.5em;
        position: absolute;
        bottom: 10px;
        color: white;
        left: 10px;
    }
    
    @media (min-width: 768px) {
        div#bigimg {
            height: 350px;
        }
    }
    
    div#bigimg {
        height: 240px;
    }
    
    #bg-title {
        top: 40%;
        width: 100%;
        text-align: center;
        position: absolute;
        color: white;
        font-family: 'Titillium Web', sans-serif;
    }
    
    #bg-text {
        position: absolute;
        width: 100%;
        top: 50%;
        color: white;
    }
    
    #bg-text>a {
        text-align: center;
        color: white;
        font-size: 1.5em;
        text-decoration: none;
        padding: 6px;
    }
    
    div#bigimg {
        top: 0px;
        min-height: 100%;
        position: relative;
        overflow: hidden;
    }
    
    div#bigimg::before {
        <% if(theme.bigimg.background=='' && theme.bigimg.color=='') {
            %>background: url('/img/bg.jpeg');
            <%
        }
        %><% if(theme.bigimg.background!='') {
            %>background: url('<%= theme.bigimg.background -%>');
            <%
        }
        %><% if (theme.bigimg.color !='') {
            %>background-color: <%=theme.bigimg.color -%>;
            <%
        }
        %>background-repeat: no-repeat;
        background-position: center top;
        background-size: cover;
        content: '';
        height: 100%;
        left: 0;
        position: fixed;
        top: 0;
        width: 100%;
        will-change: transform;
        z-index: -1;
    }
    
    .nav-bg {
        border: none !important;
        background-color: rgba(0, 0, 0, 0) !important;
    }
    
    .nav-bg .navbar-collapse>ul>li>a {
        color: white !important;
    }
    
    .nav-bg .navbar-collapse>ul>li:hover {
        color: white !important;
        border-bottom: inset;
    }
    
    .nav-bg .navbar-brand {
        color: white;
    }
</style>
